# -loan_eligibility_checker
It connects to a MySQL database to store customer and eligibility check records.  Criteria like minimum income, credit score, and EMI ratio can be updated at runtime via API.  It provides endpoints to view summaries, breakdowns by loan type, and export records as CSV.
